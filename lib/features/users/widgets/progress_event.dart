import 'package:flutter/material.dart';

class ProgressEvent extends StatelessWidget {
  const ProgressEvent({super.key, required this.isPast, this.Text});

  final bool isPast;
  final Text;
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
